import { ShieldCheck, Camera, AlertTriangle, ReceiptText, TrendingUp, Clock } from 'lucide-react';
import { StatusBadge } from '../ui/StatusBadge';
const priorities=[{title:'3 service visits awaiting verification',tone:'warning'},{title:'2 facilities need supply review',tone:'danger'},{title:'$1,250 in quotes awaiting approval',tone:'success'}];
const cards=[['Visits Today','24','7 in progress',Clock],['Proof Compliance','96%','Before/after photos',Camera],['Open Issues','8','2 high priority',AlertTriangle],['Invoices Due','$4.8k','5 pending',ReceiptText],['Facility Health','91%','Portfolio average',ShieldCheck],['Revenue Trend','+18%','Month over month',TrendingUp]];
export function MissionControl({portal}){return <section className="pageStack"><div className="heroPanel"><div><p className="eyebrow">Mission Control</p><h1>Good morning. What needs attention?</h1><p>FacilityOS reduces uncertainty by connecting work, guiding execution, verifying proof, and improving operations.</p></div><div className="heroMotto"><strong>Connect → Plan → Execute → Verify → Improve</strong><span>If it’s not verified, it’s not complete.</span></div></div>
<div className="priorityPanel"><h2>Today’s Priorities</h2><div className="priorityList">{priorities.map(p=><div className="priorityItem" key={p.title}><StatusBadge tone={p.tone}>{p.tone}</StatusBadge><span>{p.title}</span></div>)}</div></div>
<div className="metricGrid">{cards.map(([label,value,note,Icon])=><article className="metricCard" key={label}><Icon size={22}/><strong>{value}</strong><span>{label}</span><small>{note}</small></article>)}</div>
<div className="grid two"><section className="card"><h2>Verification Engine Preview</h2><p>Each service visit will guide employees through required proof: QR, SOP, checklist, before photos, after photos, issues, and manager review.</p><div className="proofList">{['QR verified','Before photos required','Checklist complete','After photos required','Manager review'].map((i,n)=><div className="proofStep" key={i}><span>{n+1}</span>{i}</div>)}</div></section><section className="card"><h2>Account Context</h2><p>Portal mode: <b>{portal}</b></p><p>This shell is role-aware. Future modules will filter records by company, profile role, customer link, employee assignment, and vendor access.</p></section></div></section>}
